# Thin caller for the org OpenRouter PR review.
# Implementation lives in RetireGolden/.github — do not copy it here.
# Separate workflow from Grok code review so a failure here cannot red
# review / grok-first-pass-gate or cancel Grok jobs.
#
# Pass only OPENROUTER_API_KEY. Do not inherit.
#
# Synchronize runs a VERIFY round (behavior since the .github#19 pin)
# that SKIPS with a notice (zero token spend) until a first-pass has
# seeded this PR's review loop; the live pin is on the uses: line below.
# Seed with workflow_dispatch (pr_number), or the PR's next reopen /
# ready_for_review. Still advisory — never a required check. Per-PR
# concurrency lives in the org reusable's own job-level groups; do not
# add a workflow-level group here (it could cancel an in-progress
# first-pass).

name: OpenRouter code review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write

jobs:
  openrouter:
    if: ${{ github.event_name == 'workflow_dispatch' || !github.event.pull_request.draft }}
    uses: RetireGolden/.github/.github/workflows/openrouter-code-review.yml@dc5b674eb14d8f32d4fb12993b450e435af02b06 # .github#24 (per-repo models roster; this repo: single x-ai/grok-4.6 lane)
    with:
      pr_number: ${{ inputs.pr_number || github.event.inputs.pr_number || github.event.pull_request.number }}
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
